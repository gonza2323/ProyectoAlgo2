from .database import Database

# Debe recorrer la base de datos y retornar
# 'vectors' es la estructura que definamos contendrá a los vectores
# No retorna nada, solo actualiza vectors
def update_vectors(database: Database, vectors, word: str):
    pass
